---
name: ado-excel-testcase-import
description: Import a test-case Excel (ALM "Pacchetto" export or ADO query export) into Azure DevOps Test Factory, one static suite per tab or into a given suite, optionally filtered by cell colour; optionally create one Epic per tab with feature/<prefix>-<tcId>-<slug> Issues. Use when someone hands over a test-case .xlsx with a plan/suite id and wants test cases or feature Issues created.
---

The full, tool-neutral instructions are in `skills/ado-excel-testcase-import/SKILL.md` at the repo
root. Read that file now and follow it exactly. Its scripts are in
`skills/ado-excel-testcase-import/scripts/`.
