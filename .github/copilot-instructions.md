# Copilot instructions

Follow [`AGENTS.md`](../AGENTS.md) at the repo root. It lists the reusable skills in
[`skills/`](../skills/). When a request matches a skill (for example "import this test-case Excel into
Azure DevOps"), read that skill's `SKILL.md` and follow it.
